### 1. `test_euclidean_similarity`

* **The Task:** Calculate the straight-line distance between two points  and .
* **The Concept:** This introduces the foundation of **clustering and classification**. In data science, "distance" often measures how similar two data points are to one another.
* **Python Skill:** Importing `math`, using `math.sqrt()`, and handling multiple parameters.

### 2. `test_investment_growth`

* **The Task:** Calculate the future value of an account using the compound interest formula: .
* **The Concept:** Focuses on **exponential growth**, which is essential for understanding trends in finance, population, or infectious disease modeling.
* **Python Skill:** Using the exponent operator (`**`) and order of operations (PEMDAS).

### 3. `test_min_max_scaler`

* **The Task:** Take a raw value and "scale" it to a range between 0 and 1 based on a provided minimum and maximum.
* **The Concept:** This is **Data Normalization**. It ensures that features with large ranges (like "Income") don't overpower features with small ranges (like "Age") in a machine learning model.
* **Python Skill:** Division, subtraction, and handling edge cases (like avoiding division by zero).

### 4. `test_timestamp_parser`

* **The Task:** Convert a large number of total seconds into a human-readable format (Days, Hours, Minutes).
* **The Concept:** Teaches **Data Wrangling**. Data Scientists rarely receive clean dates; they often have to parse raw integer timestamps or "seconds since" values.
* **Python Skill:** Mastering Floor Division (`//`) and the Modulo operator (`%`).

### 5. `test_sentiment_binner`

* **The Task:** Assign a "Priority Score" (1, 2, or 3) to a customer based on their satisfaction rating and the size of their transaction.
* **The Concept:** Introduces **Categorical Binning**. This is the process of turning continuous numerical data into discrete "bins" or groups for easier analysis.
* **Python Skill:** Boolean logic and `if-elif-else` conditional structures.